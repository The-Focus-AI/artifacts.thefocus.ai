{
  "name": "@the-focus-ai/artifacts",
  "version": "1.2.1",
  "description": "CLI-first Artifact publishing service for TheFocus.AI",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "bin": {
    "artifacts": "dist/src/cli.js"
  },
  "files": [
    "dist/src",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/The-Focus-AI/artifacts.thefocus.ai.git"
  },
  "bugs": {
    "url": "https://github.com/The-Focus-AI/artifacts.thefocus.ai/issues"
  },
  "homepage": "https://artifacts.thefocus.ai",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "artifacts": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "build:review-editor": "esbuild scripts/review-editor-entry.js --bundle --format=esm --minify --outfile=public/vendor/review-editor.js",
    "prepack": "pnpm run build",
    "lint": "prettier --check package.json tsconfig.json vitest.config.ts vercel.json 'src/**/*.ts' 'api/**/*.ts' 'tests/**/*.ts' README.md 'docs/**/*.md' 'public/**/*.html' && tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:storage:live": "node -e \"for (const name of ['DATABASE_URL','BLOB_READ_WRITE_TOKEN']) if (!process.env[name]) { console.error(name + ' is required for live storage tests'); process.exit(1) }\" && vitest run tests/storage-live.test.ts --reporter verbose",
    "test:storage:live:db": "node -e \"if (!process.env.DATABASE_URL) { console.error('DATABASE_URL is required for live database storage tests'); process.exit(1) }\" && vitest run tests/storage-live.test.ts --reporter verbose -t 'live Neon/Postgres'",
    "test:storage:live:blob": "node -e \"if (!process.env.BLOB_READ_WRITE_TOKEN) { console.error('BLOB_READ_WRITE_TOKEN is required for live Blob storage tests'); process.exit(1) }\" && vitest run tests/storage-live.test.ts --reporter verbose -t 'live Vercel Blob'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "dependencies": {
    "@clerk/backend": "^3.4.11",
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.4.0",
    "markdown-it": "^14.3.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@tiptap/core": "^2.27.2",
    "@tiptap/extension-image": "2.27.2",
    "@tiptap/extension-table": "2.27.2",
    "@tiptap/extension-table-cell": "2.27.2",
    "@tiptap/extension-table-header": "2.27.2",
    "@tiptap/extension-table-row": "2.27.2",
    "@tiptap/pm": "^2.27.2",
    "@tiptap/starter-kit": "^2.27.2",
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.1",
    "happy-dom": "^20.10.6",
    "prettier": "^3.8.3",
    "tiptap-markdown": "^0.9.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
