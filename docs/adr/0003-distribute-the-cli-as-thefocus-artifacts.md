# Distribute the CLI as @the-focus-ai/artifacts

Artifacts v1 will distribute its command-line publisher as the npm package `@the-focus-ai/artifacts` with an `artifacts` executable. npm distribution keeps local publishing simple through `npx @the-focus-ai/artifacts publish <path>` while leaving room for a standalone binary later if usage justifies the release complexity.
