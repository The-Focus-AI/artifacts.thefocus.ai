# Distribute the CLI as @thefocus/artifacts

Artifacts v1 will distribute its command-line publisher as the npm package `@thefocus/artifacts` with an `artifacts` executable. npm distribution keeps local publishing simple through `npx @thefocus/artifacts publish <path>` while leaving room for a standalone binary later if usage justifies the release complexity.
